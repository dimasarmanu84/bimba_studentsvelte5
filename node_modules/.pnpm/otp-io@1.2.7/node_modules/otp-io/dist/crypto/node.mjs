import {createHmac,randomFillSync}from'crypto';const hmac = async function hmac(algorithm, key, message) {
    return createHmac(algorithm, key).update(message).digest();
};
const randomBytes = function randomBytes(size) {
    return randomFillSync(new Uint8Array(size));
};export{hmac,randomBytes};